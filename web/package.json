{
  "name": "@gyaku/web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "lint": "prettier . --check",
    "format": "prettier . --write"
  },
  "devDependencies": {
    "prettier": "3.9.6",
    "typescript": "6.0.3",
    "vite": "8.2.1"
  }
}
