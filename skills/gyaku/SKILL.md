---
name: gyaku
description: Wire up, extend, or test dependency injection with @gyaku/di, a tiny TypeScript DI container built around `await using`. Use when adding services/registries with createRegistry, resolving a dependency graph, stubbing dependencies for tests via replaceService/replaceValue, or adapting positional functions/classes with asFunctionArgs/asClass/asClassArgs.
license: MIT
metadata:
  author: mkizka
  version: "1.0.0"
---

# @gyaku/di

Tiny, type-safe DI container for TypeScript. Before writing or reviewing code that uses `@gyaku/di`, read its README for the full API, patterns, and gotchas:

```
node_modules/@gyaku/di/README.md
```

If the package isn't installed yet, or the README can't be found locally, fetch it instead:

https://raw.githubusercontent.com/mkizka/gyaku/main/packages/gyaku/README.md
